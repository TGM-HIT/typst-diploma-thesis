/// The TGM logo. This is a partially applied function and thus can accept most of the parameters
/// that ```typc image()``` can.
///
/// -> content
#let tgm-logo = image.with("tgm_logo.svg")

/// The HTL logo. This is a partially applied function and thus can accept most of the parameters
/// that ```typc image()``` can.
///
/// -> content
#let htl-logo = image.with("htl-bildung-mit-zukunft.png")

/// The Just Do IT HIT logo. This is a partially applied function and thus can accept most of the
/// parameters that ```typc image()``` can.
///
/// -> content
#let hit-logo = image.with("jdIT_logo_hit_black.png")
